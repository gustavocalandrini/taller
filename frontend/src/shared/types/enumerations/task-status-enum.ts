export type TaskStatus = 'STARTED' | 'COMPLETED' | 'PAUSED';

export const TaskStatus = {
    STARTED: 'STARTED' as TaskStatus,
    COMPLETED: 'COMPLETED' as TaskStatus,
    PAUSED: 'PAUSED' as TaskStatus
};
